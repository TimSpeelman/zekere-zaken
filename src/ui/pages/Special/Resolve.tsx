import Box from "@material-ui/core/Box";
import React from 'react';

interface Props {
}

export const Resolve: React.FC<Props> = ({ }) => {

    return (
        <Box pt={3}>
            We proberen verbinding te maken met de andere gebruiker..
        </Box>
    )
}
